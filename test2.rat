
[*comments are great*]
function convert1x (fahr int)
{
    return 5 * (fahr -32)/9;
}

function made_up_function ()
{
    if (true /= false)
    {
        a = 3.2;
        b = 1.0;
        c = 42 + b;
        return a + b + c;
    }
    otherwise 
    {
        return false;
    }
}

%%
int low, high, step; [*more comment*]

get(low, high, step);
while (low <= high)
{
    put(low);
    put(convert1x(low));
    low = low + step;
}
%%